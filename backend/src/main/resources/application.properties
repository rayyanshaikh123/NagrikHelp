# Application
spring.application.name=civic-issues-backend
server.port=8081

# MongoDB Atlas
# Replace <username>, <password>, and <cluster-host> with your Atlas credentials
spring.data.mongodb.uri=mongodb+srv://rayyanshaikhh:rayyanshk@cluster0.vgg3w.mongodb.net/NagrikHelp?retryWrites=true&w=majority
spring.data.mongodb.auto-index-creation=true

# Logging
logging.level.org.springframework.security=INFO
# AI feature flags
# ai.enabled=true        # master switch for external AI calls
# ai.heuristic.enabled=true  # enable heuristic fallback when AI disabled or fails
# ai.ocr.enabled=true     # run local OCR before sending prompt (disable if native libs missing)
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent   # default (will retry v1 on 404)
# gemini.api.url=https://generativelanguage.googleapis.com/v1/models/gemini-2.5-flash:generateContent       # explicit v1 endpoint
# gemini.api.url=https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent       # older flash model
# gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent         # legacy pro example (may require access)
# Gemini API Key (prefer env var GEMINI_API_KEY in production)
# gemini.api.key=YOUR_GEMINI_API_KEY
# JWT settings
gemini.model=gemini-2.5-flash
gemini.api.key=AIzaSyCcbgMXkraG2gK5IMuXusJJEVTCYlU3IZk  # NOTE: Loaded from application.properties as requested
app.jwt.secret=change-me-very-secret-key-change-me-very-secret-key
# 7 days in milliseconds
app.jwt.expiration-ms=604800000
ai.ocr.autoDisableOnError=false
# Always retry OCR each request while setting up native libs
ai.ocr.retryEachRequest=true
# If native lib fails but you want to attempt every request (no permanent disable), set:
# ai.ocr.retryEachRequest=true
# Mail (optional) - uncomment and fill to enable outgoing emails
#spring.mail.host=smtp.example.com
#spring.mail.port=587
#spring.mail.username=your-smtp-user
#spring.mail.password=your-smtp-password
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.smtp.starttls.required=true
#spring.mail.properties.mail.debug=false
# OCR / Tesseract configuration
# Set tesseract.datapath to directory containing tessdata folder (NOT the tessdata folder itself)
# Example macOS (homebrew): /opt/homebrew/Cellar/tesseract/5.4.1/share
 # Homebrew symlink may not expose tessdata directly; set to actual Cellar path containing 'tessdata' folder
 tesseract.datapath=/opt/homebrew/Cellar/tesseract/5.4.1/share
 tesseract.lang=eng
 tesseract.nativeLibPath=/opt/homebrew/opt/tesseract/lib
# If native libs are not found (UnsatisfiedLinkError libtesseract), set tesseract.nativeLibPath to directory containing libtesseract.dylib (macOS Homebrew usually /opt/homebrew/opt/tesseract/lib)
# tesseract.nativeLibPath=/opt/homebrew/opt/tesseract/lib
# Default admin seeding (dev only)
app.admin.email=superadmin@nagrikhelp.com
app.admin.password=rayyan7
app.admin.name=Super Admin